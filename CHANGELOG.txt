CHANGELOG — About Page Navigation Fix (Verification Pass 2)
==============================================================

WHAT I FOUND
------------
I checked the actual index.html and about.html files currently in this
project, and independently re-checked the exact contents of the ZIP I
delivered in the previous turn (unzipped and grepped it directly). In
both cases, the "About & Philosophy" link was already correctly enabled:
no aria-disabled="true", no Coming Soon badge, no href="#", no
tabindex="-1", and js/navigation.js's click-prevention logic only
targets a[aria-disabled="true"] — which this link has never had.

This means the file you delivered previously already worked. The most
likely explanations on your end are browser caching (the browser served
an old cached copy of index.html) or an older index.html still sitting
in your local/deployed folder that wasn't overwritten when the About
files were added. I can't verify your local environment directly, so I
can't confirm which — but I can confirm the source file itself has been
correct for two deliveries running.

WHAT CHANGED IN THIS RELEASE
------------------------------
To give you a distinctive, easy-to-verify marker that you're looking at
the current file (not a cached one), I applied your exact requested
markup:

  index.html:
    <a href="about.html" class="dropdown-link">About & Philosophy</a>

  about.html:
    <a href="about.html" class="dropdown-link" aria-current="page">About & Philosophy</a>

This is functionally identical to the previous version (no aria-disabled
either way) but now carries class="dropdown-link", which you can search
for directly in the file to confirm you have this exact build.

No other change was made to either file.

VERIFIED BEFORE DELIVERY
-------------------------
[x] index.html "About & Philosophy" link: no aria-disabled, no Coming Soon
    badge, no href="#", no tabindex="-1", no disabled class
[x] about.html "About & Philosophy" link: same, plus aria-current="page"
[x] Career Vision & Strategy remains aria-disabled="true" with Coming Soon
    on both pages (no separate page exists for it)
[x] Profile remains a <button class="dropdown-toggle"> — a dropdown
    trigger, not a direct link — on both pages
[x] Searched styles/*.css for the exact combined selector pattern
    ".coming-soon, [aria-disabled=\"true\"] { pointer-events: none; }" —
    does not exist. The only aria-disabled rule sets
    pointer-events: auto (components.css line 35), and the only
    pointer-events: none in the codebase is on a decorative ::before
    background pattern in about.css, unrelated to any link.
[x] js/navigation.js click-prevention selector is
    a[aria-disabled="true"] — confirmed this no longer (and never
    correctly did) match the About link
[x] HTML tag-balance check on both files — zero errors
[x] No other navigation behavior changed: dropdowns remain click-only,
    single-open-at-a-time, close on outside click/Escape/link-select —
    js/navigation.js was not modified
[x] No Home or About page content or design modified — only the two
    lines shown above changed
